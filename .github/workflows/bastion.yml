name: bastion

# Bastion reviewing Bastion. On every pull request this downloads the pinned,
# published `bastion` binary (BASTION_VERSION below) and runs `bastion review`
# over the changeset; a blocked review exits non-zero and fails the job. Branch
# protection on `main` requires the `bastion review` check so a blocking gate
# stops the merge.
#
# The reviewer engine is a *released* binary, not one built from the PR's own
# sources. That is deliberate: building from the PR would let a change edit the
# very engine that judges it -- neutering a reviewer or forcing a pass. Pinning to
# a published, CODEOWNERS-governed release keeps the engine outside the PR's reach,
# and bumping BASTION_VERSION is itself a reviewed change. Reviewer *policy*
# (bastion/reviewers.yaml) is still read from the checkout so prompt and trigger
# changes take effect in the same PR -- those paths are CODEOWNERS-protected too.
#
# This is the concrete, self-hosted MVP of the adapter described in docs/developer-guide/github-adapter.md.
# It does not yet post per-reviewer check runs or inline PR comments (that is the
# packaged `review-action`/GitHub App, still future); here the job's pass/fail is
# the gate and the full run is uploaded as an artifact.

on:
  pull_request:
    types: [opened, synchronize, reopened]

# Read-only: this MVP only needs to read the diff and fail the job. The future
# comment-posting adapter will additionally need `pull-requests: write` and
# `checks: write`.
permissions:
  contents: read

# One in-flight review per PR; a new push cancels the previous run.
concurrency:
  group: bastion-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  # The released bastion engine that reviews this repo's PRs -- the governed
  # reviewer version. Bump it (a CODEOWNERS-reviewed change) to adopt a newer
  # engine; it must be a published release tag with attached binaries.
  BASTION_VERSION: v0.1.0

jobs:
  review:
    name: bastion review
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # The Codex subscription credentials live in an Actions secret, and GitHub
    # does not share secrets with workflows triggered by fork PRs. We also will
    # not run an agentic backend over untrusted code. So this runs for same-repo
    # PRs only; a maintainer reviews a fork PR by pushing its branch to a trusted
    # branch in this repo.
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - uses: actions/checkout@v7
        with:
          # Full history so the base branch resolves for the changeset diff and so
          # the Codex reviewer can run `git diff <base>` over the working tree.
          fetch-depth: 0

      - name: Install the pinned bastion release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          asset="bastion-x86_64-unknown-linux-gnu.tar.gz"
          dest="$RUNNER_TEMP/bastion"
          mkdir -p "$dest"
          gh release download "$BASTION_VERSION" \
            --repo "${{ github.repository }}" \
            --pattern "$asset" \
            --dir "$dest"
          tar -xzf "$dest/$asset" -C "$dest"
          # The archive extracts to a directory named after the asset stem.
          echo "$dest/${asset%.tar.gz}" >> "$GITHUB_PATH"

      - name: Verify bastion
        run: bastion --version

      - uses: actions/setup-node@v6
        with:
          node-version: 20

      - name: Install the Codex CLI
        run: npm install -g @openai/codex

      - name: Authenticate Codex as the PR author
        env:
          # Bastion bills each PR's reviews to its author's own Codex subscription
          # (docs/developer-guide/github-adapter.md, "Authentication & billing"). The author's GitHub login
          # maps to a `CODEX_AUTH_<LOGIN>` secret holding the contents of that
          # person's `~/.codex/auth.json`; Codex refreshes the access token from the
          # stored refresh token at run time. To onboard a contributor, add their
          # secret and a `case` arm below. The mapping lives here, in a
          # CODEOWNERS-protected workflow, so changing who may spend a subscription
          # is itself a reviewed change. An unmapped author fails closed (the gate
          # blocks) rather than silently billing someone else.
          #
          # Bot authors (dependabot[bot]) have no subscription of their own, so
          # their PRs bill to a maintainer who has opted in to sponsor them -- a
          # ToS-compliant arrangement where the maintainer governing the bot powers
          # the review of its changes. Note that GitHub serves secrets to
          # Dependabot-triggered runs from the separate *Dependabot* secret store,
          # not the Actions store, so any CODEX_AUTH_<LOGIN> a bot arm references
          # must also be set as a Dependabot secret (gh secret set --app dependabot)
          # or it arrives empty and the gate blocks.
          AUTHOR: ${{ github.event.pull_request.user.login }}
          CODEX_AUTH_JSSBLCK: ${{ secrets.CODEX_AUTH_JSSBLCK }}
        run: |
          author="$(printf '%s' "$AUTHOR" | tr '[:upper:]' '[:lower:]')"
          # Quote bracketed bot logins; unquoted, case reads [bot] as a glob class.
          case "$author" in
            jssblck) cred="$CODEX_AUTH_JSSBLCK" ;;
            'dependabot[bot]') cred="$CODEX_AUTH_JSSBLCK" ;;
            *)
              echo "::error::No Codex subscription is mapped for PR author '$AUTHOR'. Add a CODEX_AUTH_<LOGIN> secret and a case arm in .github/workflows/bastion.yml." >&2
              exit 1
              ;;
          esac
          if [ -z "$cred" ]; then
            echo "::error::Codex credential for '$AUTHOR' is mapped but its secret is empty; set the matching CODEX_AUTH_<LOGIN> repository secret." >&2
            exit 1
          fi
          mkdir -p "$HOME/.codex"
          printf '%s' "$cred" > "$HOME/.codex/auth.json"
          chmod 600 "$HOME/.codex/auth.json"

      - name: Run bastion review
        env:
          # Keep run history inside the workspace so it can be uploaded as an
          # artifact instead of landing in the runner's per-user data directory.
          BASTION_DATA_DIR: ${{ github.workspace }}/.bastion
          # Resolve the `codex` binary npm just put on PATH.
          BASTION_CODEX_BIN: codex
        # Default GitHub bash runs with `-eo pipefail`, so a blocked review's
        # non-zero exit fails the job (and the required check). The human-readable
        # stream goes to the log; the canonical JSONL is persisted under
        # BASTION_DATA_DIR and uploaded below.
        run: bastion review --base "origin/${{ github.base_ref }}"

      - name: Upload the run
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: bastion-run
          path: ${{ github.workspace }}/.bastion/runs/**
          if-no-files-found: warn
