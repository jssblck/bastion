{
  "name": "bastion-site",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Marketing site for Bastion (bastion.jessica.black).",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.0",
    "@fontsource-variable/jetbrains-mono": "^5.2.5",
    "@fontsource-variable/schibsted-grotesk": "^5.2.5",
    "astro": "^7.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.3.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
